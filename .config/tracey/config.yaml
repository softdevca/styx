specs:
  - name: parser
    prefix: r
    include:
      - docs/content/spec/parser.md
    impls:
      - name: rust
        include:
          - "**/*.rs"

  - name: schema
    prefix: r
    include:
      - docs/content/spec/schema.md
    impls:
      - name: rust
        include:
          - "**/*.rs"

  - name: diagnostics
    prefix: r
    include:
      - docs/content/spec/diagnostics.md
    impls:
      - name: rust
        include:
          - "**/*.rs"
